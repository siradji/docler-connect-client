const initialState = {
  username: null,
  room: null,
  messgae: '',
  messages: null,
  user: null,
  users: null,
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
